@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}

<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-4 mb-3">
            <i class="bi bi-database-fill-gear me-3"></i>
            SQL SERVER TOOLS
        </h1>
        <p class="lead text-muted">選擇一個工具開始使用 / Select a tool to get started</p>
    </div>

    <!-- Features Grid -->
    <div class="row g-4">
        <!-- Data Dictionary Generator Card -->
        <div class="col-md-6 col-lg-4">
            <div class="feature-card h-100">
                <div class="card-body d-flex flex-column">
                    <div class="feature-icon mb-3">
                        <i class="bi bi-file-earmark-text-fill"></i>
                    </div>
                    <h3 class="card-title h5 mb-3">Data Dictionary Generator</h3>
                    <p class="card-text text-muted flex-grow-1">
                        自動產生資料庫的 Data Dictionary 文件（Markdown 格式），包含完整的資料表結構、欄位資訊及說明。
                    </p>
                    <div class="mt-3">
                        <a href="/DDGenerator" class="btn btn-primary w-100">
                            <i class="bi bi-arrow-right-circle me-2"></i>
                            開始使用
                        </a>
                    </div>
                </div>
                <div class="feature-status">
                    <span class="badge bg-success">
                        <i class="bi bi-check-circle-fill me-1"></i>可用
                    </span>
                </div>
            </div>
        </div>

        <!-- Placeholder Card 1 - Coming Soon -->
        <div class="col-md-6 col-lg-4">
            <div class="feature-card h-100 coming-soon">
                <div class="card-body d-flex flex-column">
                    <div class="feature-icon mb-3">
                        <i class="bi bi-magic"></i>
                    </div>
                    <h3 class="card-title h5 mb-3">AI 欄位說明生成器</h3>
                    <p class="card-text text-muted flex-grow-1">
                        使用 AI 技術自動為資料表欄位生成詳細的說明文字，提升文件完整性。
                    </p>
                    <div class="mt-3">
                        <button class="btn btn-secondary w-100" disabled>
                            <i class="bi bi-hourglass-split me-2"></i>
                            即將推出
                        </button>
                    </div>
                </div>
                <div class="feature-status">
                    <span class="badge bg-warning">
                        <i class="bi bi-clock-fill me-1"></i>開發中
                    </span>
                </div>
            </div>
        </div>

        <!-- Placeholder Card 2 - Coming Soon -->
        <div class="col-md-6 col-lg-4">
            <div class="feature-card h-100 coming-soon">
                <div class="card-body d-flex flex-column">
                    <div class="feature-icon mb-3">
                        <i class="bi bi-bar-chart-line-fill"></i>
                    </div>
                    <h3 class="card-title h5 mb-3">資料庫分析工具</h3>
                    <p class="card-text text-muted flex-grow-1">
                        分析資料庫結構，提供效能優化建議、索引分析及關聯圖表視覺化。
                    </p>
                    <div class="mt-3">
                        <button class="btn btn-secondary w-100" disabled>
                            <i class="bi bi-hourglass-split me-2"></i>
                            即將推出
                        </button>
                    </div>
                </div>
                <div class="feature-status">
                    <span class="badge bg-warning">
                        <i class="bi bi-clock-fill me-1"></i>開發中
                    </span>
                </div>
            </div>
        </div>

        <!-- Placeholder Card 3 - Coming Soon -->
        <div class="col-md-6 col-lg-4">
            <div class="feature-card h-100 coming-soon">
                <div class="card-body d-flex flex-column">
                    <div class="feature-icon mb-3">
                        <i class="bi bi-table"></i>
                    </div>
                    <h3 class="card-title h5 mb-3">Schema 比較工具</h3>
                    <p class="card-text text-muted flex-grow-1">
                        比較兩個資料庫的結構差異，自動產生同步用的 SQL 指令稿。
                    </p>
                    <div class="mt-3">
                        <button class="btn btn-secondary w-100" disabled>
                            <i class="bi bi-hourglass-split me-2"></i>
                            即將推出
                        </button>
                    </div>
                </div>
                <div class="feature-status">
                    <span class="badge bg-warning">
                        <i class="bi bi-clock-fill me-1"></i>開發中
                    </span>
                </div>
            </div>
        </div>

        <!-- Placeholder Card 4 - Coming Soon -->
        <div class="col-md-6 col-lg-4">
            <div class="feature-card h-100 coming-soon">
                <div class="card-body d-flex flex-column">
                    <div class="feature-icon mb-3">
                        <i class="bi bi-code-slash"></i>
                    </div>
                    <h3 class="card-title h5 mb-3">SQL 產生器</h3>
                    <p class="card-text text-muted flex-grow-1">
                        根據資料表結構自動產生常用的 CRUD SQL 語句及預存程序範本。
                    </p>
                    <div class="mt-3">
                        <button class="btn btn-secondary w-100" disabled>
                            <i class="bi bi-hourglass-split me-2"></i>
                            即將推出
                        </button>
                    </div>
                </div>
                <div class="feature-status">
                    <span class="badge bg-warning">
                        <i class="bi bi-clock-fill me-1"></i>開發中
                    </span>
                </div>
            </div>
        </div>

        <!-- Placeholder Card 5 - Coming Soon -->
        <div class="col-md-6 col-lg-4">
            <div class="feature-card h-100 coming-soon">
                <div class="card-body d-flex flex-column">
                    <div class="feature-icon mb-3">
                        <i class="bi bi-shield-check"></i>
                    </div>
                    <h3 class="card-title h5 mb-3">資料庫安全檢查</h3>
                    <p class="card-text text-muted flex-grow-1">
                        檢查資料庫的安全設定，包含權限配置、加密狀態及最佳實踐建議。
                    </p>
                    <div class="mt-3">
                        <button class="btn btn-secondary w-100" disabled>
                            <i class="bi bi-hourglass-split me-2"></i>
                            即將推出
                        </button>
                    </div>
                </div>
                <div class="feature-status">
                    <span class="badge bg-warning">
                        <i class="bi bi-clock-fill me-1"></i>開發中
                    </span>
                </div>
            </div>
        </div>
    </div>

    <!-- Info Section -->
    <div class="row mt-5">
        <div class="col-12">
            <div class="info-card">
                <div class="card-body">
                    <div class="row align-items-center">
                        <div class="col-md-8">
                            <h4 class="mb-2">
                                <i class="bi bi-lightbulb-fill me-2" style="color: var(--warning-color);"></i>
                                持續開發中
                            </h4>
                            <p class="mb-0 text-muted">
                                我們正在不斷添加新功能以提升您的開發效率。如有任何建議或需求，歡迎與我們聯繫！
                            </p>
                        </div>
                        <div class="col-md-4 text-md-end mt-3 mt-md-0">
                            <a href="https://github.com/hollowtimTW/SqlServerDDGenerator" target="_blank" class="btn btn-outline-primary">
                                <i class="bi bi-github me-2"></i>
                                訪問 GitHub
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    /* Feature Card Styles */
    .feature-card {
        background: var(--bg-card);
        backdrop-filter: blur(20px);
        border: 1px solid var(--border-color);
        border-radius: 16px;
        box-shadow: var(--shadow-md);
        overflow: hidden;
        transition: all 0.3s ease;
        position: relative;
        cursor: pointer;
    }

    .feature-card:hover {
        transform: translateY(-8px);
        border-color: var(--accent-color);
        box-shadow: 0 12px 48px rgba(138, 180, 248, 0.2);
    }

    .feature-card.coming-soon {
        opacity: 0.7;
    }

    .feature-card.coming-soon:hover {
        opacity: 0.85;
        transform: translateY(-4px);
    }

    .feature-icon {
        width: 64px;
        height: 64px;
        background: var(--gradient-primary);
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        color: white;
        box-shadow: 0 8px 24px rgba(102, 126, 234, 0.3);
    }

    .feature-card.coming-soon .feature-icon {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.3) 0%, rgba(118, 75, 162, 0.3) 100%);
        box-shadow: none;
    }

    .feature-status {
        position: absolute;
        top: 1rem;
        right: 1rem;
    }

    .info-card {
        background: var(--bg-card);
        backdrop-filter: blur(20px);
        border: 1px solid var(--border-color);
        border-radius: 16px;
        box-shadow: var(--shadow-md);
        overflow: hidden;
    }

    .display-4 {
        font-size: 2.5rem;
        font-weight: 700;
        background: var(--gradient-primary);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    /* Animation */
    .feature-card {
        animation: fadeInUp 0.6s ease-out;
    }

    .feature-card:nth-child(1) { animation-delay: 0.1s; }
    .feature-card:nth-child(2) { animation-delay: 0.2s; }
    .feature-card:nth-child(3) { animation-delay: 0.3s; }
    .feature-card:nth-child(4) { animation-delay: 0.4s; }
    .feature-card:nth-child(5) { animation-delay: 0.5s; }
    .feature-card:nth-child(6) { animation-delay: 0.6s; }

    @@keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .display-4 {
            font-size: 2rem;
        }
        
        .feature-icon {
            width: 56px;
            height: 56px;
            font-size: 1.75rem;
        }
    }
</style>
